{
  "name": "@universal-login/commons",
  "version": "0.3.4",
  "description": "Unviersal Login Common source code",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn clean",
    "build": "tsc",
    "clean": "rm -fr dist",
    "pretest": "yarn build",
    "test": "mocha -t 10000 \"dist/test/**/*.js\"",
    "lint": "tslint -p tsconfig.json \"{lib,test}/**/*.{ts,js}\""
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "ens-builder": "^2.0.1",
    "sinon": "^7.2.3",
    "sinon-chai": "^3.3.0",
    "ts-node": "^8.0.2",
    "tslint": "^5.16.0",
    "typescript": "^3.3.3"
  }
}
