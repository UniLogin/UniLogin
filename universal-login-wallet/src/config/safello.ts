const LANGUAGE = 'en';
const COUNTRY = 'other';
const CRYPTO = 'eth';

export const safelloStagingUrl = 'https://app.s4f3.io/sdk/quickbuy.html?appId=1234-5678' +
  '&border=true' +
  '&address-helper=true' +
  `&lang=${LANGUAGE}` +
  `&country=${COUNTRY}` +
  `&crypto=${CRYPTO}`;
